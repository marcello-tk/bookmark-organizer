info:
  name: create-user
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3001/users
  body:
    type: json
    data: |-
      {
        "name": "Joe",
        "email": "joe@mail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​