info:
  name: update-bookmark
  type: http
  seq: 9

http:
  method: PUT
  url: http://localhost:3001/users/1/bookmarks/1
  body:
    type: json
    data: |-
      {
        "title": "Mody Dick",
        "url": "https:\\dicks-at-moby.com",
        "userId": "1",
        "description": "hello"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​