info:
  name: create-tag-for-user
  type: http
  seq: 12

http:
  method: POST
  url: http://localhost:3001/users/1/tags
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
